/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  env: {
    FAL_KEY: process.env.FAL_KEY,
  },
}

module.exports = nextConfig 